using UnityEngine;
using Homework.Interface;

namespace Homework.Mechanics.Character
{
    public class CharacterIdentity : MonoBehaviour, Icharacter
    {
        public bool SetActiveWhiteboard { get; set; }

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
